<div class="container mt-4">
  <div class="card shadow">
    <div class="card-header bg-primary text-white">
      <div class="d-flex justify-content-between align-items-center">
        <div>
          <h4 class="mb-0"><i class="fas fa-edit me-2"></i>تعديل سجل الحضور</h4>
          <p class="mb-0 mt-1">
            <i class="fas fa-id-card me-2"></i>رقم اللاعب: <%= playerId %>
            <span class="badge bg-light text-dark ms-2">
              <i class="fas fa-user me-1"></i><%= attendance.player.name %>
            </span>
          </p>
        </div>
        <div>
          <span class="badge bg-light text-dark">
            <i class="fas fa-calendar-day me-1"></i>
            <%= attendance.date.toLocaleDateString('ar-EG') %>
          </span>
        </div>
      </div>
    </div>
    
    <div class="card-body">
      <form action="/attendance/<%= attendance._id %>?_method=PUT" method="POST">
        <div class="row">
          <div class="col-md-6 mb-3">
            <label class="form-label fw-bold"><i class="fas fa-calendar-alt me-2"></i>تاريخ التدريب</label>
            <input type="date" name="date" class="form-control form-control-lg"
                   value="<%= attendance.date.toISOString().split('T')[0] %>" required>
          </div>

          <div class="col-md-6 mb-3">
            <label class="form-label fw-bold"><i class="fas fa-user-clock me-2"></i>حالة الحضور</label>
            <select name="status" class="form-select form-select-lg" required>
              <% statusOptions.forEach(option => { %>
                <option value="<%= option %>" <%= attendance.status === option ? 'selected' : '' %>>
                  <% if (option === 'present') { %>
                    <i class="fas fa-check-circle text-success me-2"></i>حاضر
                  <% } else if (option === 'late') { %>
                    <i class="fas fa-clock text-warning me-2"></i>متأخر
                  <% } else { %>
                    <i class="fas fa-times-circle text-danger me-2"></i>غائب
                  <% } %>
                </option>
              <% }) %>
            </select>
          </div>
        </div>

        <div class="mb-4">
          <label class="form-label fw-bold"><i class="fas fa-comment-dots me-2"></i>ملاحظات</label>
          <textarea name="comment" class="form-control" rows="3" 
                    placeholder="أدخل أي ملاحظات إضافية..."><%= attendance.comment || '' %></textarea>
        </div>

        <div class="d-flex justify-content-between flex-column flex-sm-row gap-2">
          <div class="d-flex gap-2">
            <button type="submit" class="btn btn-primary btn-lg flex-grow-1">
              <i class="fas fa-save me-2"></i>حفظ التعديلات
            </button>
            <a href="/attendance/player/<%= attendance.player._id %>" class="btn btn-secondary btn-lg flex-grow-1">
              <i class="fas fa-arrow-right me-2"></i>العودة
            </a>
          </div>
          
          <button type="button" class="btn btn-danger btn-lg flex-grow-1" data-bs-toggle="modal" data-bs-target="#deleteModal">
            <i class="fas fa-trash-alt me-2"></i>حذف السجل
          </button>
        </div>
      </form>
    </div>
  </div>
</div>

<!-- Delete Confirmation Modal -->
<div class="modal fade" id="deleteModal" tabindex="-1" aria-labelledby="deleteModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header bg-danger text-white">
        <h5 class="modal-title" id="deleteModalLabel">
          <i class="fas fa-exclamation-triangle me-2"></i>تأكيد الحذف
        </h5>
        <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        <p class="lead">هل أنت متأكد من حذف سجل الحضور هذا؟</p>
        <p class="text-muted">هذا الإجراء لا يمكن التراجع عنه.</p>
        <p class="fw-bold">رقم السجل: <%= attendance._id %></p>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
          <i class="fas fa-times me-2"></i>إلغاء
        </button>
        <form action="/attendance/<%= attendance._id %>?_method=DELETE" method="POST" class="d-inline">
          <button type="submit" class="btn btn-danger">
            <i class="fas fa-trash-alt me-2"></i>نعم، احذف
          </button>
        </form>
      </div>
    </div>
  </div>
</div>